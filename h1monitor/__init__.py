# The one place the version lives — pyproject reads it, /start shows it, and
# the update check compares it against the newest GitHub release. Bump it in
# the release commit, then tag that commit v<same number>.
__version__ = "0.2.0"
